// Code generation — placeholder
import type { PatternCard } from '../store.svelte'

export function generateCardCode(_card: PatternCard): string {
  return ''
}

export function generateProjectCode(_cards: PatternCard[]): string {
  return ''
}
